<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0,  minimum-scale=1, maximum-scale=1">
    <title>Guess The Number</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;600&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/65c28df05f.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/stylesheets/index.css">
    
</head>
<body>
    <div id="contain">
        <div class="row">
            <div id="guide" class="grid-item bgDivider">
                <h1><i class="fas fa-scroll"></i> Guide <i class="fas fa-scroll"></i></h1>
                <div id="guideContent">
                    <h3><i class="fas fa-gamepad"></i> Gameplay</h3>
                    <p>Lose 1 <i class="health fas fa-heart"></i> on each wrong guess</p>
                    <p>GAME OVER when <i class="health fas fa-heart"></i> reaches 0</p>
                    <p>A new Round will start on every successful guess</p>
                    <h3><i class="fas fa-cog"></i> Difficulties</h3>
                    <p><i class="iconLeft far fa-grin-beam"></i> <span>Easy</span> - Gain 1 <i class="health fas fa-heart"></i> and 100 Points on a successful guess</p>
                    <p><i class="iconLeft far fa-grin-beam-sweat"></i> <span>Normal</span>- Gain 2 <i class="health fas fa-heart"></i> and 300 Points on a successful guess</p>
                    <p><i class="iconLeft far fa-sad-cry"></i> <span>Hard</span> - Gain 3 <i class="health fas fa-heart"></i> and 600 Points on a successful guess</p>
                </div>
                
                <a href="#mainDiv"><h3>Let's Play!</h3></a>
                <a href="#highScoreDiv"><h3>View Highscores</h3></a>
            </div>
            <div id="mainDiv" class="grid-item midBg">
                <h1 id="title">GUESS THE NUMBER<span id="labelNum">1-100</span></h1>
                <div id="roundDiv">
                    <h2>Round: <span id="numRound">-</span></h2>
                    <div id="cheatActivate">
                        <p>? = <span id="cheatMod"></span> </p>
                    </div>
                </div>
                <div class="divider" id="difficultyOptions">
                    <h4 id="easy" class="displayInline difficulty">Easy</h4>
                    <h4 id="normal" class="displayInline difficulty">Normal</h4>
                    <h4 id="hard" class="displayInline difficulty">Hard</h4>
                </div>
                <div id="healthDiv" class="divider">
                    <!-- <label class="displayInline" for="health"><i class="health fas fa-heart"></i> Health</label> -->
                    <p class="displayInline" name="health" id="health"></p>
                </div>
                <div class="previewGuess">
                    <div>
                        <p>Previous Guesses:</p>
                        <span id="previousGuess">-</span>
                    </div>
                    <div>
                        <p>Correct Guesses</p>
                        <span id="previousCorrect">-</span>
                    </div>
                </div>
                <div id="startGame">
                    <input type="submit" value="Start" id="startButton">
                </div>
                <div id="gameInputs">
                    <input class="displayBlock" type="number" id="userGuess" autocomplete="off">
                    <input type="submit" value="Submit" id="submitGuess">
                    <input type="submit" value="Exit" id="resetGame">
                </div>
                <h4 id="lastResult"></h4>
                <span id="correctNumber"></span>
                </div>
                <h3 id="ptsPosition">Points: <span id="points">0</span></h3>
            <div id="highScoreDiv" class="grid-item bgDivider">
                <h1><i class="fas fa-trophy"></i> Highscore <i class="fas fa-trophy"></i></h1>
                <div class="rowTwo">
                    <div>
                        <h2>Name</h2>
                        <ol>
                        <% for(let i=0; i < 15 && i < players.length; i++){ %>
                            <% for(let keyName in players[i]){ %>
                               <% if(keyName == 'name') { %>
                                    <li><%= players[i][keyName] %></li>
                               <% } %>
                            <% } %>
                        <% } %>
                        </ol>
                        
                    </div>
                    <div>
                        <h2>Score</h2>
                        <ul id="playerScores">
                            <!-- <% players.forEach((player)=> { %>
                                <li><%= player.points%></li>
                            <% }) %> -->
                            <% for(let i=0; i < 15 && i < players.length; i++){ %>
                                <% for(let keyName in players[i]){ %>
                                   <% if(keyName == 'points') { %>
                                        <li><%= players[i][keyName] %></li>
                                   <% } %>
                                <% } %>
                            <% } %>
                        </ul>
                    </div>
                </div>
                <div class="navLinks">
                    <a class="toGuide" href="#guide">Read the Guide</a>
                    <a class="toMain" href="#mainDiv">Let's Play</a>
                </div>
            </div>
        </div>
        <div id="myModal" class="modal" tabindex="-1">
            <div class="modal-content">
                <span class="close">&times;</span>
                <h3>Please enter your name</h3> 
                <form action="/" method="POST">
                    <input type="text" name="playerName" id="playerName" maxlength="10" autofocus>
                    <input type="submit" value="Submit" id="submitName">
                    <input type="text" name="playerScore" id="playerScore">
                </form>
            </div>
        </div>
    </div>
    
    <script src="/scripts/index.js"></script>
</body>
</html>